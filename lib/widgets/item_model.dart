class ItemModel{
  final String productId, productName, productPrice, productImages;
  const ItemModel({
    required this.productId,
   required this.productImages,
   required this.productName,
   required this.productPrice,
});
}